# 队列修复与首页前置准备计划

更新：2026-09-25。工作分支：`feature/home`。

进度按「实现完成 / 自动化通过 / 人工待验收」分项记录。人工闭环未完成时，不把该步骤标成已验收。

| 步骤 | 实现 | 自动化 | 人工 |
| --- | --- | --- | --- |
| 1 条目标识、队列算法与快照 | 完成 | 步骤 1 提交时构建、60 项单元测试、lint 通过 | 已见重复条目；完整验收未完成 |
| 2 命令、控制器与 entryId 控制 | 完成 | 构建、63 项单元测试、lint 通过 | 待验收 |
| 3 有界传输与队列状态分批 | 完成 | JDK 17 构建、66 项单元测试、lint 通过 | 模拟器空队列检查通过；非空队列及恢复待验收 |
| 4 普通/FM 状态边界 | 完成（仅基础边界） | JDK 17 构建、68 项单元测试、lint 通过 | 普通空队列检查通过；FM 业务未接入 |

整表替换已改为私有临时文件引用，队列状态按版本分批读取；4,000 首合成列表已通过自动化验证。真实日推/歌单及其分页业务尚未接通，完整人工验收仍未完成。

依赖关系：[核心交互规范](PLAYBACK_INTERACTIONS.md) → **本计划** → [首页功能计划](HOME_PLAN.md)。核心规范决定行为，本计划负责把现有代码迁移到该行为；首页不能继续依赖旧的 hash 去重队列。

## 目标与范围

在保留单个 MediaSessionService / ExoPlayer 的前提下，完成重复条目、四种播放操作、精确控制与旧记录兼容，为完整日推/歌单和 FM 提供可复用基础。

本阶段不接推荐接口、不开发首页内容或云端歌单、不请求真实账号反馈，不实现 FM 补歌，也不引入数据库、通用命令框架或第二套播放器。保留已有音频权限判断、试听裁剪、歌词数据层、账号隔离与请求取消机制。

## 现有问题与对应改动

下表记录改造前的问题基线，不代表步骤 1–4 实现后的现状。路径相对 `android/app/src/main/java/io/github/xiangyuplayer/`。

| 位置 | 已确认现状 | 必须完成的改动 |
| --- | --- | --- |
| `playback/PlaybackQueue.kt` | play 遇到已有 hash 直接选中；insertNext 移除旧条目，当前歌曲直接忽略 | 新增独立条目，保留重复；增加完整列表替换 |
| `playback/PlaybackProtocol.kt`、`PlaybackService.kt` | 点选、移除、当前项基于 hash；没有列表替换命令 | 命令与状态传递 entryId，明确四种操作 |
| `ui/playback/QueueSheet.kt` | LazyColumn key、当前项高亮、选中及移除使用 hash | 按 entryId 展示并操作，重复歌曲只高亮当前一条 |
| `ui/search/SearchScreen.kt` | 当前歌曲“下一首播放”被禁用 | 移除该限制，同一首可重复插入 |
| `ui/playback/PlaybackViewModel.kt` | 断连重试调用 play(song)；seek 只比较 Song；待连接点播只存 Song | 重试/恢复不得走新增操作；定位操作校验条目，保留待执行操作的明确意图 |
| `data/playback/PlaybackSnapshot.kt` | v1 快照要求 hash 唯一，遍历及当前项也是 hash | 升级快照并迁移 v1，允许重复歌曲、要求 entryId 唯一 |
| `PlaybackProtocol.extras` / `PlaybackViewModel.update` | 发布、读取整份队列；状态轮询也重复解码队列 | 队列内容与高频播放状态分开同步，处理大列表消息体积 |
| `data/playback/PlaybackStateStore.kt` | 读取文件有 4 MiB 上限 | 同步核对新格式与大队列读写边界，不能写成功却在重启时因超限删除 |
| 现有 `PlaybackQueueTest` | 固定断言去重、移动旧项和忽略当前歌曲插入 | 改为已确认交互，保留仍有效的顺序、随机及循环测试 |

## 步骤 1：条目标识、队列算法与快照迁移

这是一次连贯的数据模型迁移，不能只去掉去重判断。

- 增加轻量 `QueueEntry(entryId, song)`，Song 的 hash/专辑等仍用于业务请求；每次插入产生新的不透明唯一 ID，恢复时沿用保存的 ID，不因 Compose 重组重新生成。
- 队列保存 entries、currentEntryId 和按 entryId 排列的遍历顺序。选择、移除、上下首、随机及单曲循环均精确到条目。删除一条相同歌曲不影响另一条。
- 插入位置是当前项之后；没有当前项时为队首。随机模式同时更新实际遍历顺序，保证下一首确实是插入项；单曲循环仍只影响自然结束，不改变手动切歌的含义。
- 完整列表替换接受有序歌曲集合和选中索引，逐项生成 ID，保留重复、设为顺序播放。先验证非空和索引合法再原子替换；非法输入不清空当前队列。
- 快照升级到 v2。读取 v1 时为每个旧条目生成 ID，并把原 hash 当前项/遍历顺序映射到对应 ID，保留模式、进度、试听起点和会话归属。成功迁移后按正常原子保存流程写 v2；未知版本或损坏内容明确按既有恢复错误策略处理，不能编造可用队列。
- v2 校验 entryId 唯一、遍历完整、当前项存在，允许相同 hash 多次出现。只有活动队列，不增加进入 FM 前的旧队列备份。

完成标准：队列算法与记录读写能表达重复歌曲，迁移不丢失合法 v1 状态。临时兼容代码不得在最终交付中重新引入按 hash 选择条目的歧义。

进度：实现完成。自动化通过（构建、60 项单元测试、lint）。人工：已看到重复歌曲条目；完整验收未完成。

## 步骤 2：服务命令、控制器与界面同步迁移

建议使用语义直接的操作命名，最终名称可随代码风格调整：

| 操作 | 输入 | 行为 |
| --- | --- | --- |
| insertAndPlay | Song | 新增下一项并立即播放，不查找旧 hash |
| enqueueNext | Song | 只新增下一项，当前播放/暂停状态不变；空队列选中新增项但暂停 |
| replaceAndPlay | 完整列表引用、选中索引 | 校验后整表替换并播放指定项，不用逐首命令模拟 |
| selectEntry | entryId | 切换既有项，不新增或重排 |

- 同步修改 PlaybackProtocol、PlaybackService、PlaybackViewModel、QueueSheet 和搜索回调，移除搜索对当前 hash 的插入禁用。队列移除也使用 entryId。
- 当前项状态及 MediaItem.mediaId 使用 entryId；歌曲 hash 仍通过 Song 提供给音频解析及歌词查询，不能把 entryId 当作酷狗歌曲 ID。
- 重试与进程恢复按当前条目重新解析，不调用 insertAndPlay。控制器重连后重新核对条目、会话及待执行意图；目标已消失则不执行，不通过“补插一首”伪装恢复。
- 拖动进度与歌词点击携带操作发起时的 entryId，服务端执行时再校验当前项，防止拖动期间切换到另一条相同歌曲后误定位。仅在 ViewModel 比较 Song 不足以保证这一点。
- PlaybackRequests 现有取消加请求版本校验继续复用；重复歌曲同 hash 也必须是不同播放请求。队列替换、切歌和账号变化使旧解析失效。
- 歌词内容可继续按 hash 复用；拖动状态、歌词跟随等与当前播放项有关的 UI 状态应按 entryId 重置，不强制重复下载相同歌词。
- 页面、系统通知和耳机上下首继续走服务的一套规则，不在 UI 维护第二份可独立修改的队列。

完成标准：搜索、下一首、队列点选/移除和重试形成端到端闭环，重复条目不会导致列表 key 冲突、多条高亮或误删；现有歌词和试听定位保持可用。

进度：实现完成。重试与定位在命令到达服务时按 entryId 核对；整表替换先完整解析并校验，再改变队列或停止播放。自动化通过（构建、63 项单元测试、lint）。人工待验收，不把本步骤标成已验收。本步最初用小型列表验证协议和服务入口；该临时整表 Bundle 协议已在步骤 3 被文件引用方案替换。

## 步骤 3：完整列表传输与异步替换边界

日推取消 30 首上限，后续云端歌单还可能分页，必须同时处理“整表导入”和“队列状态返回”的消息体积，不能只优化其中一边。

- 保持 UI 通过 MediaController 操作服务。建议采用应用私有的临时列表文件加不透明引用提交大列表：IO 写入完整元数据，命令只携带引用、选中索引和请求版本；服务只解析自己管理的引用，不接受任意路径或外部 URI。
- 传输数据仅为歌曲元数据，不含音频 URL 或凭据；临时记录按会话隔离，成功、取消、退出及启动清理孤立文件。复用小型文件工具，不搭建通用传输框架。
- 队列返回状态采用版本号与有界分批读取：会话 extras 只放当前项、数量、模式、队列版本等小状态，队列界面按需读取。读取中版本变化需丢弃旧批次，不混合不同队列；进度轮询不重复解码整表。
- 上述是优先方案；实施时若用更简单的有界分页协议，也必须满足原子提交、双向消息有界、会话隔离和取消清理，不能用“限制为前 30 首”回避问题。
- 私有文件和恢复快照的体积校验必须读写一致。以实际编码尺寸核对限制，超限明确报告，不静默截断、不保存一个无法恢复的成功记录。
- 为列表替换保留请求版本：取齐分页、读取完整元数据和校验完成前继续旧播放；失败不改队列。其间新的点播、选择、插入、清空或账号变化使旧的待替换操作失效，不能晚到覆盖用户的新选择。
- 原子替换完成后才解析选中歌曲的音频；此后如果无权限或解析失败，保留新队列并显示真实错误，不假装旧音频属于新歌曲。

完成标准：大列表不会通过单个巨大 Bundle 往返；整表按顺序到达，选中索引可靠，取消/失败不留下半份队列。分页业务接口由日推/音乐库阶段接入，本阶段只准备通用的完整列表入口。

### 步骤 3 实施与验证记录（2026-09-25）

- `data/playback/QueueTransferStore` 只读写应用私有目录内的 UUID 引用；文件包含会话缓存归属与歌曲元数据，不含播放地址或认证。写入、读取在 IO 上执行并校验会话归属；成功、失败、取消后清理，账号切换/退出和服务启动清理遗留文件。
- `QueueReplacement` 保存服务签发的替换请求标识，`QueueTransferSession` 管文件校验与原子提交。校验完成前保持旧播放；新点播、下一首插入、选择、删除、清空、切歌、系统停止、断连或账号变化使旧请求失效。音频解析在队列提交后开始，解析失败保留新队列。
- `PlaybackProtocol` 的整表 Bundle 入口已移除。会话 extras 仅发布当前项、数量、模式、版本等小状态；`QueueController` 仅在队列面板打开时分批读取，关闭时取消，版本变化时丢弃旧批次。只在列表完整且条目标识唯一时展示，加载和失败重试独立于音频播放。
- `QueuePages` 按实际 UTF-8 编码字节数限制每页最多 24 KiB；即使 Parcel 使用 UTF-16，仍留有消息头空间。超大单条元数据拒绝，不截断列表。临时文件与恢复快照均限制为 4 MiB；替换前检查加入 entryId 和遍历顺序后的实际快照尺寸，并为后续时长等字段预留 1 KiB。持久化写入超过限制时报告保存失败，不覆盖原有有效记录。
- 自动化：移除旧小列表协议的一项测试，新增 4 项必要回归，合计 66 项。覆盖 4,000 首含重复项的列表完整顺序、指定位置、分页尺寸、快照恢复、跨版本/不完整批次拒绝、迟到请求/账号失效、非法引用/内容，以及“传输文件可容纳但恢复快照超限”的失败边界。JDK 17 下 `assembleDebug / testDebugUnitTest / lintDebug` 通过；lint 无错误，保留已有依赖、Manifest 和未使用资源警告。
- 已在开发模拟器更新调试 APK 并确认首页、队列面板及 0 首空状态正常。当前模拟器没有可恢复的队列，因此未验证非空分页、播放与重启恢复；没有添加演示歌曲或使用真实账号做自动化云端写操作。大列表入口当前由测试覆盖，日推/音乐库页面尚不能触发它。

人工复测：登录并使用现有搜索重复点播几首，打开队列确认数量、顺序和单项高亮；点选/删除一条同歌记录，关闭再打开队列；暂停时插入下一首，确认仍暂停；强制停止并重开，确认重复条目和进度恢复且不出声。再核对试听歌词定位及通知栏上下首。真实大列表整表替换和业务分页需在日推/歌单入口接入后补验；步骤 4 进度见下方记录。

## 步骤 4：为首页保留明确的状态边界

- 普通/FM 属于队列会话类型，与顺序/随机/单曲循环分开。基础阶段整理命令边界，FM 具体状态、推荐来源字段与补歌任务随 HOME_PLAN 接入，不伪造尚不存在的 FM 功能。
- 插入和点选操作不隐式改变会话类型；完整列表替换明确给出目标类型。日推/歌单替换到普通，FM 首批替换到 FM。
- 队列条目 ID 不能兼任 FM 推荐 ID。接 FM 时再根据真实响应增加必要的推荐来源数据，自动补歌去重与用户插入重复分开。
- FM 进入不保存旧队列、不提供恢复入口或 FM 清空功能；普通队列现有清空仍可保留。模式相关的按钮和服务端命令限制在 FM 阶段一起落实。
- 不为上述准备新建模块、通用策略工厂或复杂继承体系；优先围绕现有 Queue / Service 职责完成改动。

### 步骤 4 实施与验证记录（2026-09-25）

- 在现有 `PlaybackQueue` 中增加 `QueueSessionType.NORMAL / FM`，与 `PlaybackMode` 的遍历规则分开。插入并播放、下一首插入、队列点选均保持类型，整表替换必须显式传入目标类型，清理活动队列时重置为普通类型。不创建第二套队列、旧队列备份或策略框架。
- `PlaybackViewModel → QueueController → PlaybackProtocol → QueueTransferSession` 传递明确的替换目标；服务在签发替换请求时绑定目标，缺失或未知类型拒绝。文件读取与尺寸校验期间仍保持旧队列及类型，提交时一次更新；旧 FM 替换请求失效后不能覆盖新的普通队列意图。
- 会话 extras 与 `PlaybackUiState` 提供活动类型，供后续首页接入。快照随活动队列保存类型；旧 v1 迁移和缺少字段的 v2 按普通队列兼容，显式 null 或未知值拒绝，不猜测模式。现有启动流程仍只恢复为暂停。
- 本阶段没有 FM 入口、联网、补歌、反馈、推荐 songid 或推荐来源字段；entryId 仍仅标识队列条目。未来日推/歌单显式传 NORMAL，FM 首批显式传 FM。FM 的播放规则、清空/模式按钮及对应服务命令限制仍需随 HOME_PLAN 的 FM 阶段共同接入，不能把当前类型字段称为可用 FM 功能。
- 在既有测试中扩展跨类型迟到请求和 v1 断言，新增 2 项必要测试，验证插入/点选保持类型、整表替换明确切换、活动队列清理与恢复、旧 v2 兼容、未知类型拒绝；共 68 项单元测试通过。JDK 17 下 `assembleDebug / testDebugUnitTest / lintDebug` 通过。
- 已更新开发模拟器调试包，启动及普通空队列面板检查通过，保留清空和顺序播放入口。模拟器当前无可恢复的非空队列，非空播放、试听歌词与进程恢复仍需人工验收；FM 边界本阶段仅用合成数据测试，没有进行真实账号云端写操作。

人工复测仍按上方步骤 3 的清单：重复点歌、点选/删除其中一条、暂停时插入下一首、切换普通播放模式、强制停止重开后保持暂停、试听歌词定位与通知栏切歌。待 FM 业务接通后再验收搜索插入保持 FM、日推/歌单整表替换退出 FM，以及相关按钮和补歌取消行为。步骤 1–4 实现完成不等于首页前置阶段已完成人工验收。

## 验证与提交安排

建议一个完整基础修复提交覆盖步骤 1–2 及必要兼容，另一个完整准备提交覆盖步骤 3–4；具体可随规模合并，但不能交付“内存允许重复、UI/快照仍要求唯一 hash”的中间状态。

优先修改现有测试，不为每个转发方法写测试。必要覆盖如下，允许一项测试包含同类边界：

1. 搜索重复点歌保留原条目、产生新 ID 并选中新项；下一首插入不改变当前项，当前歌曲也能再次插入；随机模式中插入确实位于实际下一项。
2. 按 entryId 选中/移除重复项只影响目标；整表替换含重复歌曲时仍选中指定位置，非法输入保留原队列。
3. 重试和控制器恢复不新增条目；同歌不同项切换后旧 seek/解析不能作用于新项。扩展既有请求取消测试，不依赖真实网络。
4. v1 迁移保留模式、顺序、进度和试听起点；v2 重复条目往返后当前项精确；账号不匹配记录继续拒绝。
5. 用一份有代表性的合成大列表检查有界传输、完整顺序、版本变化及失败不半替换，不做大量尺寸组合或压力基准。

完成代码改动后，在 `android/`、JDK 17 下执行：

```bash
./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug
```

通过后进行一次针对性模拟器检查，不重复扩大测试：搜索同一首多次，队列中只高亮当前条目，点选/删除其中一条，暂停时插入下一首，重启后确认重复队列与进度恢复且不出声。再人工核对非零起点试听歌词跳转及通知栏上下首。完整列表入口在首页未接通前用测试验证，不往主应用添加演示歌单或临时调试入口。

## HOME_PLAN 开始前的完成条件

- [ ] 搜索与队列操作符合核心规范，所有条目身份统一到 entryId。
- [ ] 重试、恢复、定位与重复条目正确配合，已有试听/歌词逻辑没有回退。
- [ ] v1 兼容和 v2 保存可用，旧会话清理仍有效。
- [x] 完整列表替换具备原子性与有界传输，状态返回不再每次广播整表（合成大列表自动化通过，真实业务入口待接入）。
- [ ] 必要测试、构建、lint 和针对性检查完成，并如实记录限制。
- [x] README 与本计划更新实际状态，现有旧规则测试及文档不会被误用为新验收要求。

以上完成后才能将首页的每日推荐接入新队列入口。接口文档阅读可先进行；真实日推/FM业务与反馈继续按 HOME_PLAN 分阶段实施。
