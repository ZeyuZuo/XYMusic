# 相遇音乐 · XYMusic

面向 Android 的轻量酷狗第三方音乐播放器。使用 Kotlin、Jetpack Compose、Material 3 和 Media3，视觉参考 NeriPlayer。

最终目标是安装 APK 即可使用，由 Android 客户端直接访问酷狗官方服务，用户无需部署后端。当前开发阶段暂用 KuGouMusicApi 中转联调；后续将所需接口逐步替换为自有 Kotlin 实现。当前开发版仍依赖中转服务，不代表已经实现直连。

## 版本管理

仓库与 Gradle 工程名称统一为 `XYMusic`，App 中文名为“相遇音乐”。远程仓库：`git@github.com:ZeyuZuo/XYMusic.git`，主分支为 `main`。本地目录和 Android 包名暂时沿用原名称，不影响仓库名称。

`.gitignore` 排除本机 SDK 配置、构建缓存、签名凭据、环境配置及两个上游独立仓库；Gradle Wrapper 随源码提交。从 GitHub 克隆后，如需启动 API 或参考 NeriPlayer，请按 `services/README.md`、`references/README.md` 的来源和基线提交另行获取上游仓库。

## 工作区

```text
android/                   新 App；Android Studio 打开此目录
docs/                      架构、复用来源与后续计划
services/KuGouMusicApi/     原 API 仓库，独立运行、保留 Git 历史
references/NeriPlayer/     原播放器仓库，仅作为参考、保留 Git 历史
AGENTS.md                  后续开发代理的工作约定
LICENSE                    GPL-3.0
```

两个上游仓库均已原样移动。它们不参与新 App 的构建，并由根 `.ignore` 和 `.gitignore` 隔离。显式查看上游代码用 `rg --no-ignore`。没有删除原始代码或仓库历史。

## 当前进度

已实现：四栏导航、分组设置页与账号卡片、手机号验证码登录界面与请求流程、Android Keystore 加密会话保存、恢复时的认证检查、失效后单次刷新、退出与服务地址隔离。详见 [登录实现与验收说明](docs/LOGIN.md)。

**用户复测反馈登录已恢复正常；已实现歌曲、歌单、歌手三类在线搜索，已接通基础单曲播放、迷你播放器与完整播放页，基础 LRC 歌词已接通，每日推荐和 FM 播放/有限续播已实现（设备交互待人工验收），FM 反馈和云端音乐库尚未接通。** 同手机号多账号暂支持手动指定账号 ID，自动账号列表未接入。首页每日推荐进入独立列表，猜你喜欢启动或返回 FM，不展示假数据。

2026-09-17：在 `login` 分支实现上述登录流程。基于上游源码核对字段，MockWebServer 测试不使用真实账号。用户已反馈登录完成；重启恢复、过期刷新等场景仍需逐项验收。v0.1 范围见 [产品与实施计划](docs/V0.1.md)。

登录失败现提供失败阶段与受限数字错误码，避免把所有拒绝响应都解释为风控；相关构建、20 项单元测试及 lint 通过，用户反馈暂未再次出现拒绝；若复发再根据错误码排查，原因尚未确认。

登录后界面展示真实头像、昵称与状态标签，以及粉丝、关注、访客数量；资料缺失显示“—”，失败支持独立重试。账号操作收进菜单，开发服务配置默认折叠。浅色、深色与 1.5 倍字体 Preview 已检查，示例数据仅用于预览。

头像与统计阶段：已核对 `/user/detail` 的真实响应字段，并在开发模拟器确认头像 HTTPS 加载与三项数量展示。完整构建、27 项单元测试及 lint 通过；资料不做持久缓存，详情列表后续实现。

### 搜索阶段（2026-09-17）

`feature/search-playback` 分支先实现搜索：输入关键词后点击按钮或键盘搜索，切换歌曲、歌单、歌手标签查看对应结果，支持清空、手动加载更多、空结果与失败重试。列表每条独立排列，标题、完整歌手列表和专辑信息允许换行，不设置固定行高或单行截断。歌曲结果已支持单曲点播，歌单详情和歌手主页跳转尚未实现；搜索历史后续补齐。

搜索复用当前账号的 API 会话，退出、账号或服务地址变化会取消请求并清除结果。已读取三类真实响应核对字段；歌曲通过用户批准的开发模拟器已登录会话做一次只读查询。临时字段检查入口已删除，自动化测试仅用合成数据与 MockWebServer。JDK 17 下 `assembleDebug / testDebugUnitTest / lintDebug` 通过，共 32 项单元测试（新增 5 项）。模拟器已安装最终调试包并检查搜索入口与三个分类标签；完整结果页交互与更多关键词留待用户手动验收。

### 播放阶段 1（2026-09-17）

已实现搜索歌曲点播、迷你播放器、暂停/继续与失败重试，接入 Media3 后台、通知栏及锁屏控制。每次点歌替换当前单曲；会话或服务地址变化会停止并清空播放。完整播放与试听分开处理，试听受返回时间范围限制。用户已确认点歌可听、会员歌曲显示试听、非会员歌曲正常播放；后台/锁屏及耳机等设备场景仍待验收，其他试听格式按实际响应逐步补充。

播放源解析、服务生命周期和界面控制分离；构建、41 项单元测试及 lint 通过。详细边界与手动验收清单见 [播放计划](docs/PLAYBACK_PLAN.md)。完整播放页已在阶段 2 实现；队列与切歌已在阶段 3 实现；进程恢复已在阶段 4 实现，基础歌词已在阶段 5 实现。

### 播放阶段 2（2026-09-17）

已接入完整播放页：点击迷你播放器进入，显示大封面、完整歌名和歌手、实际进度与总时长，支持拖动定位、暂停/继续和失败重试。顶部返回与系统返回不停止播放。试听以播放器实际受限时长为准，未知时长或不可定位时禁用拖动。

JDK 17 下构建、41 项单元测试及 lint 通过；最终调试包已安装到在线模拟器并检查启动。完整页、试听定位、旋转及锁屏控制同步仍待手动验收，步骤见 [播放计划](docs/PLAYBACK_PLAN.md)。已提供浅色/深色大字体 Preview，未进行渲染验收。

### 播放阶段 3（2026-09-17）

已接入本地播放队列：点击搜索歌曲仅将该首插入并播放，已有歌曲直接切换，不批量导入搜索结果。搜索右侧三点菜单支持下一首播放；队列弹窗支持点选、移除和确认清空，完整页支持上下首、顺序/随机/单曲循环。标准系统媒体切歌命令接入相同队列规则，账号或服务变化清空队列；本阶段不修改云端歌单，也不做进程重启恢复。

构建、49 项单元测试和 lint 通过。模拟器已检查菜单、逐首入队、下一首播放、队列显示、试听结束自动推进、完整页下一首及模拟器上一首媒体键；锁屏/通知栏、实体耳机及异常场景仍需验收。规则与步骤见 [播放计划](docs/PLAYBACK_PLAN.md)。

### 播放阶段 4（2026-09-18）

已实现队列、当前歌曲、模式/随机顺序和进度的本地保存。重启仅恢复为暂停，点击播放时重新获取音频并继续进度；试听恢复按实际允许范围校验。可能失效的音频地址最多自动重新解析一次，断网或恢复失败保留队列并支持手动重试。退出账号清理保存记录，旧写任务不能回写到新会话。

构建、53 项单元测试和 lint 通过，仅新增 4 项必要测试；模拟器已确认强制停止后恢复 0:17 进度且保持暂停，点击播放可重新解析并继续试听。真实地址过期及设备异常组合场景仍需人工验收，步骤见 [播放计划](docs/PLAYBACK_PLAN.md)。临时音频链接和凭据不写入播放记录；歌词见下方阶段 5。

### 非零起点试听修复（2026-09-18）

修复 Perfect、Love Story 等歌曲因试听起点不为 0 而提示无法识别播放信息的问题。播放器按服务返回的原曲起止位置裁剪，试听页仍显示片段内的进度，拖动与恢复限制在允许范围。构建、54 项单元测试及 lint 通过；模拟器已确认 Perfect 正常进入 60 秒试听，Taylor Swift 页面复测遇到搜索超时，仍待手动确认。详情见 [播放计划](docs/PLAYBACK_PLAN.md)。

### 播放阶段 5（2026-09-18）

已接入歌词搜索、下载和本地 Base64/LRC 解码。完整播放页可切换封面/歌词，支持当前行高亮、自动滚动、点击歌词跳转；手动浏览后可点击“跟随当前歌词”恢复跟随。加载、无歌词、网络失败及格式错误分别展示，歌词失败不打断音频。

试听使用原曲时间轴高亮，点击歌词时减去试听起点，范围外禁止跳转；起点随队列记录保存。旧版试听记录缺少起点时，等待用户播放并重新解析后再高亮。只读取歌词，不修改云端账号数据。JDK 17 下构建、58 项单元测试及 lint 通过（新增 4 项歌词测试）；模拟器已确认 Perfect 试听歌词加载、点击跳到片段 0:15 且保持暂停。其他人工验收见 [播放计划](docs/PLAYBACK_PLAN.md)。

### 首页前置：队列步骤 3（2026-09-25）

完整列表通过应用私有临时文件传入，服务校验会话归属、完整内容和恢复快照尺寸后才原子替换；新的队列操作会使旧替换请求失效。队列面板按版本读取最多 24 KiB 的分页，进度轮询不再解码整表；加载失败可独立重试。临时文件和恢复快照均设 4 MiB 上限，超限明确失败，不静默截断。

JDK 17 下构建、66 项单元测试和 lint 通过，包括 4,000 首合成列表的顺序、重复项、分页、选中位置及恢复验证。调试包已更新到开发模拟器，首页和空队列面板检查通过；模拟器当前无可恢复队列，非空分页与播放恢复仍需人工验收。日推/云端歌单入口尚未接通，不能据此称首页已经可用。实现边界与人工步骤见 [队列准备计划](docs/QUEUE_PREPARATION_PLAN.md)。

### 首页前置：队列步骤 4（2026-09-25）

队列会话类型（普通/FM）已与顺序/随机/单曲循环分离。插入和点选保持原类型，整表替换必须明确目标类型，并与新队列一起提交；活动类型随快照保存，旧记录兼容为普通队列。仅准备状态和命令边界，没有新增 FM 入口、推荐请求、补歌或反馈，也不保存进入 FM 前的旧队列。

JDK 17 下构建、68 项单元测试和 lint 通过，模拟器启动及普通空队列检查通过。非空播放和进程恢复仍需人工复测；FM 按钮/命令限制及真实业务交互随首页阶段实现。详见 [队列准备计划](docs/QUEUE_PREPARATION_PLAN.md)。

### 首页阶段 0：最小接口核对（2026-09-26）

已获授权复用开发模拟器会话，各读取一次日推和 FM 首批：两次业务成功，`data.song_list` 分别为 30/5 条；保留脱敏结构，不保存凭据、真实歌曲标识或完整响应。已确认日推 songid 与 album_audio_id 不同，FM 顶层缺少日推中的专辑音频 ID 和封面字段；日期/计数语义、曲长单位及 FM 补歌/反馈仍有待核实项。

用合成数据验证了中转缓存不区分账号、请求体和方法的问题，并确认 `X-Apicache-Bypass: 1` 可跳过缓存，后续推荐请求采用此方式。临时核对工具已移除，没有新增主应用业务或修改上游；详细证据与后续条件见 [首页接口核对记录](docs/HOME_API_CHECK.md)。

### 首页阶段 1：每日推荐（2026-09-26）

已实现日推列表、实际日期/数量、完整列表点播、全部播放及下一首播放；浏览和刷新不改变队列。新增按会话/服务地址隔离的元数据缓存，同日复用、跨天重取，刷新失败保留旧列表并标记来源。协议严格检查完整性，不限 30 首、不丢弃重复项，使用 bypass 头跳过中转缓存。

日推日期、条数及秒单位已通过额外授权的两次只读核对确认。构建、79 项单元测试及 lint 通过，模拟器首页检查通过；真实列表播放、断网与账号切换等交互待人工验收。实现边界、证据及操作步骤见 [每日推荐验收说明](docs/DAILY_RECOMMENDATION.md)。FM 尚未接入。

### 歌词切歌缓存修复（2026-09-26）

修复两分钟内切换歌曲可能显示上一首歌词的问题：中转服务按 URL 缓存，忽略 `POST /lyric` 请求体中的歌曲 ID。歌词搜索与下载现均带 `X-Apicache-Bypass: 1`，下载 key 继续仅放请求体。新增模拟该缓存行为的 A→B→A 回归测试，不使用真实账号。构建、80 项单元测试及 lint 通过；真实歌曲复测待用户确认。

人工复测：先播放陈绮贞《告诉我》并打开歌词，两分钟内切换松田圣子《青い珊瑚礁》，再切回；歌词应随歌曲更新，不能出现上一首的词。无需等待中转缓存过期。

### 首页阶段 2：FM 播放与有限续播（2026-09-26）

已接入 FM 首批及服务内串行补歌，首批失败保留原播放，已有 FM 入口仅返回播放页；剩余待播降至 2 时补充，自动推荐有界去重，空/重复/失败停止自动重试。普通列表替换、系统停止和账号变化取消旧任务；重启仅恢复暂停。FM 反馈与“不喜欢”未接入。

三次授权只读核对确认 5 首首批、5 首补充和曲长/变体字段。构建、95 项单元测试及 lint 通过；真实连续播放、锁屏、断网和恢复待人工复测，详见 [FM 实现与验收](docs/FM_PLAYBACK.md)。歌词串歌问题用户已确认修复。

## 启动 Android 工程

1. 用 Android Studio 打开 `android/`。
2. 使用 JDK 17，安装 Android SDK Platform 35。支持 Android 9（API 28）及以上设备。
3. Android Studio 会生成 `local.properties`；命令行也可设置 `ANDROID_HOME`。
4. 同步依赖后运行 `app`。命令行验证：

```bash
cd android
./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug
```

首次构建需要联网下载 Gradle 和依赖。项目不沿用 NeriPlayer 的 native、子模块或多源构建配置。

### 初始化验证记录（2026-09-16）

- Gradle 8.11.1 下载及工程配置成功；XML、字符串资源引用和版本目录检查通过。
- API 地址校验与 Cookie 会话共 6 项测试，在临时独立 JVM 工程中使用本项目原始源文件运行，通过。该验证不依赖 Android SDK。
- 初始化时未找到 Android SDK，完整 `assembleDebug / testDebugUnitTest / lintDebug` 命令在 SDK 定位阶段停止。次日用户反馈已在 Android Studio 编译运行；完整测试、lint 和业务真机验收仍需后续记录。
- 已提供 `.github/workflows/android.yml`，后续上传 GitHub 可执行 Android 构建、单元测试与 lint；该工作流本次尚未运行。

### 登录阶段验证（2026-09-17）

- `:app:assembleDebug :app:testDebugUnitTest :app:lintDebug` 通过，15 项单元测试通过（新增 9 项登录协议与会话测试）。
- 使用本机 Android Studio 的 JBR 21 与 Android SDK 35，编译目标仍为 Java/Kotlin 17；未另外验证 JDK 17 运行 Gradle。
- lint 无错误，仍有依赖版本、已有媒体服务导出及备份配置提示；账号密文位于系统不备份的目录。
- 已在在线模拟器安装并验证四栏导航、设置账号入口、登录表单、未配置服务时按钮禁用及返回设置页。
- 未使用真实手机号发送短信或登录，尚待真机验证 Keystore 保存、重启恢复及真实服务返回。

## 开发联调：启动临时 API 服务

已有 Node.js 22 的环境可使用 Corepack 和上游锁文件安装：

```bash
cd services/KuGouMusicApi
corepack pnpm install --frozen-lockfile
HOST=127.0.0.1 corepack pnpm start
```

默认端口为 3000。需要环境配置时参考上游 `.env.example`；不要提交设备标识或登录凭据。

- Android 模拟器：App 设置使用 `http://10.0.2.2:3000/`。
- USB 真机：运行 `adb reverse tcp:3000 tcp:3000`，App 设置使用 `http://127.0.0.1:3000/`。
- 局域网真机：服务使用 `HOST=0.0.0.0`，App 填电脑局域网 IP；仅在可信网络调试。
- 当前过渡版本如需通过网络分发测试，服务地址必须使用 HTTPS；这不是最终产品对用户的部署要求。Node.js 仅在开发服务端运行，不放进 APK。

设置页保存地址后，可从顶部账号卡片进入手机号登录。保存地址本身不代表服务器连通；具体接口失败会在登录或账号区显示。

## 继续开发

首页功能已建立 `feature/home` 分支，业务尚未接通。按 [首页功能实施计划](docs/HOME_PLAN.md)，先按 [队列修复与准备计划](docs/QUEUE_PREPARATION_PLAN.md) 落实 [播放列表核心交互规范](docs/PLAYBACK_INTERACTIONS.md)，完成验收后再做每日推荐和 FM。队列步骤 1–4 已实现独立条目、entryId 控制链路、私有文件整表替换、按版本分批读取和普通/FM 基础状态边界，允许重复插入。阶段 0 的两次首批及缓存核对已完成，日推/FM 业务尚未接通；人工闭环未完成，不把前置阶段标成已验收。

播放按 [播放功能实施计划](docs/PLAYBACK_PLAN.md) 分阶段推进：点歌出声 → 完整播放页 → 队列 → 恢复与稳定性 → LRC。阶段 1–5 已实现，歌词与设备组合场景仍需手动验收。

先按播放计划完成播放与歌词验收，再按 [v0.1 产品与实施计划](docs/V0.1.md) 推进推荐和云端音乐库；登录恢复和续期的剩余验收见 [登录验收说明](docs/LOGIN.md)。另见 [架构和里程碑](docs/ARCHITECTURE.md)、[第三方复用记录](docs/THIRD_PARTY.md) 和 [开发约定](AGENTS.md)。

新项目使用 GPL-3.0；复用的上游内容保留其许可，详见 `LICENSE` 和复用记录。
